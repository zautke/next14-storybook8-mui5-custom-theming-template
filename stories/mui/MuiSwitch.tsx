import React from 'react';
import {
  Switch,
  SwitchProps,
} from '@mui/material'


// eslint-disable-next-line react/prop-types
export const MuiSwitch = ({label}: any, props: SwitchProps) =>
  <Switch {...props}>

  </Switch>
