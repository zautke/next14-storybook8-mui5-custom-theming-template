import theme from '../buildBaseTheme';

const MuiPopover = {
  paper: {
    borderRadius: '3px',
    border: 'solid 1px ' + theme.palette.interface['grey-04'],
    boxShadow: '0 5px 10px 0 rgba(13, 35, 69, 0.15)',
    margin: 0,
    padding: 12,
  },
};

export default {
  MuiPopover,
};
