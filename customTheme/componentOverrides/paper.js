import theme from '../buildBaseTheme';

const MuiPaper = {
  root: {
    backgroundColor: theme.palette.common.paper_bk,
    paper: theme.palette.common.paper_bk,
  },
  rounded: {
    borderRadius: 3,
  },
};

export default {
  MuiPaper,
};
